import React from "react";

export const metadata = {
  title: "Book Drip Irrigation | Alilals Agrico",
  description:
    "Book a Drip Irrigation service with Alilals Agrico to install and maintain a drip irrigation system for efficient water use.",
};

const BookDripLayout = ({ children }) => {
  return <>{children}</>;
};

export default BookDripLayout;
